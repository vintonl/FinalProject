import { GearCategoryPipe } from './gear-category.pipe';

describe('GearCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new GearCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
